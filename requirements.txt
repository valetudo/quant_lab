# Pinned runtime requirements for quant_lab.
# For development (tests, lint, types) use: pip install -e ".[dev,ui,scraping]"

pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
statsmodels==0.14.2
pyyaml==6.0.1
duckdb==1.0.0
yfinance==0.2.40
beautifulsoup4==4.12.3
pandera==0.20.3
jinja2==3.1.4
python-dotenv==1.0.1
requests==2.32.4
tqdm==4.66.4

# UI extras
streamlit==1.36.0
plotly==5.22.0

# PDF export (Bond Ladder → PDF). reportlab only — the chart is drawn
# natively with reportlab.graphics, NOT via Plotly/kaleido (kaleido hangs
# on Windows — see _migration_log/v320_kaleido_issue.md).
reportlab==4.5.1

# Scraping extras (selenium + headless chrome driver mgmt)
selenium==4.21.0
webdriver-manager==4.0.1
flask==3.0.3

# Dev (kept here for one-command install)
pytest==8.2.2
pytest-cov==5.0.0
ruff==0.5.0
