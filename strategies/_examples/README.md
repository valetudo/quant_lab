# strategies/_examples

**Status**: reference

**Purpose**: minimal `Strategy` implementations used as templates and as engine smoke-test fixtures. NOT production strategies.

Currently:
- `DummyBuyAndHold` — equal-weight buy-and-hold on a fixed ticker list. The "Hello World" of the framework. See `docs/adding_a_strategy.md` for the walkthrough.
